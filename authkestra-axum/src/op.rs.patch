--- authkestra-axum/src/op.rs
+++ authkestra-axum/src/op.rs
@@ -73,15 +73,6 @@
     };
     let config = OpConfig::from_ref(&state);
 
-    let req = authkestra_op::handlers::authorize::AuthorizeRequest {
-        client_id: req_map.get("client_id").cloned().unwrap_or_default(),
-        redirect_uri: req_map.get("redirect_uri").cloned().unwrap_or_default(),
-        response_type: req_map.get("response_type").cloned().unwrap_or_default(),
-        scope: req_map.get("scope").cloned().unwrap_or_default(),
-        state: req_map.get("state").cloned(),
-        code_challenge: req_map.get("code_challenge").cloned(),
-        code_challenge_method: req_map.get("code_challenge_method").cloned(),
-        nonce: req_map.get("nonce").cloned(),
-    };
-
     match authkestra_op::handlers::authorize::handle_authorize(req, identity, &config, clients.as_ref(), codes.as_ref()).await {
